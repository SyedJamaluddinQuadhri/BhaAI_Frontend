import { useState } from "react";
import { ArrowRight, ShieldCheck } from "lucide-react";
import { Button } from "../../../components/ui/Button";
const steps=[["Welcome","A calmer place for everything you don't want to remember."],["What matters?","Tell BhaAI what you want help staying on top of."],["Connect email","Bring deadlines, bills and important threads into context."],["Connect calendar","Let BhaAI understand where your time goes."],["Import documents","Your important records become searchable and actionable."],["Privacy","Choose what BhaAI can access. You stay in control."],["Life summary","Meet BhaAI."]];

export function OnboardingPage(){const [step,setStep]=useState(0);const [done,setDone]=useState(false);if(done)return <div className="min-h-screen flex items-center justify-center p-6"><div className="max-w-2xl text-center"><div className="eyebrow mb-5">Meet BhaAI</div><h1 className="display">I found your life.<br/>Now let’s make it easier.</h1><div className="mt-10 grid gap-4 text-left sm:grid-cols-2"><div className="surface p-5">12 important emails</div><div className="surface p-5">7 documents</div><div className="surface p-5">4 deadlines</div><div className="surface p-5">3 recurring payments</div></div><Button className="mt-9" onClick={()=>setDone(false)}>Continue to BhaAI <ArrowRight size={16}/></Button></div></div>;const [title,body]=steps[step];return <div className="min-h-screen p-6"><div className="mx-auto flex min-h-[calc(100vh-48px)] max-w-6xl flex-col justify-between"><div className="flex items-center justify-between"><div className="font-bold tracking-tight">BhaAI</div><div className="text-xs muted">{step+1} / {steps.length}</div></div><div className="max-w-3xl py-20"><div className="eyebrow mb-5">{step===0?"Personal AI life operating system":"Setup"}</div><h1 className="display">{title}</h1><p className="mt-7 max-w-xl text-lg leading-8 muted">{body}</p>{step===5&&<div className="mt-8 flex items-center gap-3 text-sm"><ShieldCheck className="text-[var(--teal)]"/>Your permissions are always explicit.</div>}</div><div className="flex justify-between border-t border-[var(--line)] pt-6"><Button variant="ghost" disabled={step===0} onClick={()=>setStep(s=>s-1)}>Back</Button><Button onClick={()=>step===steps.length-1?setDone(true):setStep(s=>s+1)}>{step===steps.length-1?"See my summary":"Continue"} <ArrowRight size={16}/></Button></div></div></div>;}
