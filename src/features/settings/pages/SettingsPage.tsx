import { Moon, Sun, GraduationCap, Briefcase } from "lucide-react";
import { PageContainer } from "../../../components/layout/PageContainer";
import { Section } from "../../../components/shared/Section";
import { Button } from "../../../components/ui/Button";
import { useTheme } from "../../../hooks/useTheme";
export function SettingsPage(){const {theme,toggleTheme}=useTheme();return <PageContainer><Section eyebrow="Settings" title="Make BhaAI fit you."><p className="max-w-2xl text-lg muted">Choose how your personal operating system should feel and what context matters most.</p></Section><div className="space-y-0 border-y border-[var(--line)]"><div className="grid gap-4 py-7 md:grid-cols-[1fr_auto]"><div><h3 className="font-semibold">Appearance</h3><p className="mt-1 text-sm muted">Currently using {theme} mode.</p></div><Button variant="secondary" onClick={toggleTheme}>{theme==="light"?<Moon size={16}/>:<Sun size={16}/>}Toggle theme</Button></div><div className="grid gap-5 border-t border-[var(--line)] py-7 md:grid-cols-2"><div className="surface p-6"><GraduationCap size={20}/><h3 className="mt-4 text-lg font-semibold">Student Mode</h3><p className="mt-2 text-sm muted">Assignments, exams, projects, internships, placements and scholarships.</p><Button variant="secondary" className="mt-5">Use Student Mode</Button></div><div className="surface p-6"><Briefcase size={20}/><h3 className="mt-4 text-lg font-semibold">Professional Mode</h3><p className="mt-2 text-sm muted">Meetings, projects, contracts, invoices, travel and certifications.</p><Button variant="secondary" className="mt-5">Use Professional Mode</Button></div></div></div></PageContainer>;}
