import { ArrowUpRight } from "lucide-react";
import { PageContainer } from "../../../components/layout/PageContainer";
import { Section } from "../../../components/shared/Section";
const insights=[["6","deadlines in the next 14 days","Your calendar is manageable, but the internship application needs the earliest attention."],["3","documents expire this quarter","Two are insurance-related. Consider grouping their renewal reminders."],["4","recurring subscriptions","One subscription has not been used recently. Review it before the next charge."]];
export function InsightsPage(){return <PageContainer><Section eyebrow="Life insights" title="Patterns worth noticing."><p className="max-w-2xl text-lg muted">Not analytics for analytics’ sake. Small pieces of intelligence that can change what you do next.</p></Section><div>{insights.map(([number,title,body])=><div key={title} className="grid gap-6 border-t border-[var(--line)] py-9 md:grid-cols-[140px_1fr_auto]"><div className="text-6xl font-semibold tracking-[-.05em]">{number}</div><div><h3 className="text-2xl font-semibold tracking-tight">{title}</h3><p className="mt-3 max-w-2xl leading-7 muted">{body}</p></div><ArrowUpRight size={19}/></div>)}</div></PageContainer>;}
